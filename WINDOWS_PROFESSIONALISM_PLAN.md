# خطة احترافية لتطوير واجهات تطبيق الويندوز (WPF/WinUI 3)
## مكمّلة لوثيقة MASTER_WINDOWS_ERP_UI_INSTRUCTIONS.md الموجودة في مستودعك

> وثيقتك الحالية ممتازة في تعريف **ماذا** يجب أن يحتوي كل شاشة ولماذا التصميم الحالي غير مقبول (البند 11: مساحة بيضاء، Sidebar بدائي، غياب DataGrid احترافي...). هذه الخطة تضيف الطبقة التقنية التي تجعل هذه الرؤية قابلة للتنفيذ والصمود على المدى الطويل في بيئة WPF/WinUI 3 الحقيقية.

---

## المرحلة 0 — تنظيف وتنظيم المستودعات
**المدة: نصف يوم**

1. توحيد مكان وثيقة `MASTER_WINDOWS_ERP_UI_INSTRUCTIONS.md` — إبقاؤها في مكان مرجعي واحد بدل تكرارها في مستودعي الأندرويد والويندوز (خطر تعارض النسخ عند أي تحديث مستقبلي).
2. مراجعة بنية `src/` الحالية والتأكد من فصل المشاريع (Solution Projects) بوضوح: طبقة UI منفصلة عن Domain منفصلة عن Data — إن لم تكن مفصولة بعد، هذا أول شيء يجب إصلاحه قبل أي تصميم واجهات.
3. تأكيد أن `.gitignore` يستثني `bin/`, `obj/`, `*.user`, `.vs/`.

---

## المرحلة 1 — اختيار الأساس التقني للواجهة (قرار حاسم مبكر)
**المدة: قرار فوري + 2-3 أيام تجربة**

وثيقتك تذكر "WPF / WinUI 3" معًا كخيارين — هذا قرار يجب حسمه الآن لأنه يحدد كل شيء لاحقًا:

| المعيار | WPF | WinUI 3 |
|---|---|---|
| النضج والاستقرار | عالي جدًا (تقنية قديمة مجرّبة) | أحدث، بعض الفجوات لا تزال موجودة |
| المظهر الحديث (Fluent Design) | يحتاج مكتبات خارجية (ModernWpf, MahApps) | مدمج أصلاً |
| الأداء مع جداول ضخمة (DataGrid) | ممتاز مع Virtualization صحيح | جيد لكن أقل نضجًا |
| سهولة التوزيع (MSIX) | ممكن لكن يحتاج إعداد إضافي | مصمم لها أصلاً |
| توفر مكتبات Third-party (Charts, DataGrid متقدم) | واسعة جدًا (Syncfusion, DevExpress, Telerik) | أقل نضجًا نسبيًا |

**توصيتي:** لنظام محاسبي/ERP يعتمد بشدة على جداول بيانات ضخمة (فواتير، حركات مخزون) واستقرار طويل الأمد — **WPF مع مكتبة UI حديثة (مثل ModernWpf أو مكتبة تجارية مثل Syncfusion)** عادة خيار أكثر أمانًا للمشاريع التجارية الجادة حاليًا، بسبب نضج الـ DataGrid والطباعة والتوزيع. لكن القرار يعتمد أيضًا على خبرتك الحالية بأي منهما.

### معيار الإغلاق
- [ ] قرار نهائي موثق في `ANDROID_ERP_UI_UX...` (أو ملف جديد `WINDOWS_TECH_DECISION.md`) مع الأسباب.

---

## المرحلة 2 — نظام التصميم كـ ResourceDictionary حقيقي
**المدة: 3-5 أيام**

وثيقتك (بند 43-46) تسرد عناصر Design System لكن بدون آلية تقنية. في WPF/WinUI هذا يعني:

1. بناء `ResourceDictionary` مركزي (`Colors.xaml`, `Typography.xaml`, `Spacing.xaml`) يُدمج في `App.xaml` — لا ألوان أو أحجام مكتوبة مباشرة (`Hardcoded`) داخل أي `.xaml` شاشة.
2. تعريف `Style` قياسي لكل عنصر أساسي (`Button`, `TextBox`, `DataGrid`) مرة واحدة، تُطبّق تلقائيًا عبر `TargetType` بدل تكرار الـ Style في كل شاشة.
3. ألوان دلالية موحّدة (نفس المبدأ المطلوب في تطبيق الأندرويد لتناسق العلامة التجارية بين المنصتين):
   - لون "مستحق/دين" موحّد
   - لون "متزامن/غير متزامن" موحّد
   - **يجب أن تكون القيم اللونية مطابقة تمامًا (Hex) لما استُخدم في تطبيق الأندرويد.**
4. دعم Dark Mode عبر `ResourceDictionary` بديل يُبدَّل ديناميكيًا (`DynamicResource` وليس `StaticResource` في العناصر التي تتغير مع الثيم).
5. توثيق النتيجة في `WINDOWS_DESIGN_SYSTEM.md` (المذكور أصلاً في وثيقتك بند 62).

### معيار الإغلاق
- [ ] صفر لون أو حجم خط مكتوب مباشرة في أي ملف `.xaml` — كل شيء عبر `DynamicResource`/`StaticResource`.

---

## المرحلة 3 — المكونات المشتركة كـ UserControls/CustomControls
**المدة: 1-1.5 أسبوع**

وثيقتك (بند 54) تسرد قائمة مكونات مشتركة (AppShell, Sidebar, DataGrid, KPI...) — هنا التنفيذ:

1. كل مكوّن من القائمة يُبنى كـ `UserControl` مستقل بخصائص (`DependencyProperty`) واضحة قابلة لإعادة الاستخدام، وليس نسخة مكررة يدويًا في كل شاشة.
2. أولوية خاصة لمكوّن `EnterpriseDataGrid` (يُبنى فوق `DataGrid` الأساسي): يدعم Virtualization، فرز، فلترة، تصدير — يُبنى **مرة واحدة** ويُستخدم في كل شاشات الفواتير/المنتجات/العملاء.
3. استخدام Storybook-style: بناء نافذة داخلية (Debug فقط) تعرض كل المكونات المشتركة بحالاتها المختلفة لمراجعة الاتساق البصري بسرعة (نفس فكرة Compose Preview Gallery في الأندرويد، لكن بنافذة WPF منفصلة).

### معيار الإغلاق
- [ ] كل مكوّن من قائمة البند 54 مبني كمكوّن مستقل قابل لإعادة الاستخدام في أكثر من شاشة فعليًا.

---

## المرحلة 4 — الأداء (غائب عن الوثيقة الأصلية)
**المدة: مستمرة**

نظام محاسبي بجداول قد تحوي عشرات آلاف الصفوف (حركات مخزون، فواتير سنة كاملة) — هذا أهم فرق بين "يبدو جيدًا في العرض" و"يعمل فعليًا في الإنتاج":

1. تفعيل UI Virtualization في كل `DataGrid`/`ListView` (`VirtualizingStackPanel.IsVirtualizing="True"` مع `VirtualizationMode="Recycling"`) — بدونها، جدول 10,000 فاتورة سيجمّد الواجهة.
2. تحميل البيانات بشكل تدريجي (Pagination أو Infinite Scroll من قاعدة البيانات) بدل تحميل كل السجلات دفعة واحدة في الذاكرة.
3. كل عمليات القراءة من قاعدة البيانات المحلية (SQLite/EF Core) يجب أن تكون Async بالكامل (`async/await`) — لا استعلام واحد يجمّد الـ UI Thread.
4. استخدام `CollectionViewSource` بدل الفلترة اليدوية على القوائم لتفادي إعادة بناء الجدول بالكامل عند كل بحث.
5. قياس فعلي على جهاز متوسط المواصفات (ليس فقط جهاز التطوير) مع بيانات واقعية الحجم (استيراد بيانات تجريبية بعشرات آلاف السجلات واختبار الأداء عليها فعليًا).

### معيار الإغلاق
- [ ] جدول بـ 10,000+ صف يفتح ويُمرَّر بسلاسة دون تجمّد واضح.

---

## المرحلة 5 — الطباعة الاحترافية (تعميق البند 21 و6 من وثيقتك)
**المدة: 3-5 أيام**

وثيقتك تطلب Print Preview احترافي — التنفيذ التقني في WPF:

1. اختيار مسار واضح: `FixedDocument`/`FlowDocument` المدمج في WPF (مجاني لكن محدود التصميم)، أو مكتبة PDF متخصصة (QuestPDF أو مكتبة تجارية) لتصميم فواتير أدق بصريًا — التوصية: **QuestPDF** (مجانية للاستخدام التجاري الصغير، تصميم دقيق جدًا، سهلة الصيانة).
2. فصل منطق توليد مستند الطباعة عن الـ ViewModel تمامًا — طبقة `PrintDocumentBuilder` مستقلة قابلة للاختبار.
3. دعم تصدير PDF مباشرة (وليس فقط طباعة فيزيائية) — أساسي لمشاركة الفواتير عبر WhatsApp (البند 5 في وثيقتك).

### معيار الإغلاق
- [ ] فاتورة مطبوعة (PDF) تبدو كمستند مالي احترافي فعلي، لا نسخة من الشاشة.

---

## المرحلة 6 — التوطين (Localization) — غائب عن الوثيقة
**المدة: 2-3 أيام**

1. كل نص ظاهر عبر ملفات `.resx` (`Resources.ar.resx`) — لا نص مكتوب مباشرة داخل XAML أو code-behind.
2. الأرقام والعملات عبر `CultureInfo` مضبوط للعربية (فرق الأرقام الهندية/الغربية حسب إعداد النظام — نفس نقطة الأندرويد بالضبط، ويجب أن يتطابق السلوك بين المنصتين).
3. اختبار RTL الفعلي عبر `FlowDirection="RightToLeft"` على مستوى `Window` الرئيسية، والتأكد أن كل المكونات المخصصة (خصوصًا DataGrid والـ Icons الاتجاهية) تتبع الاتجاه الصحيح تلقائيًا.

### معيار الإغلاق
- [ ] صفر نص مكتوب مباشرة، والتطبيق يعمل RTL كامل بدون استثناءات بصرية.

---

## المرحلة 7 — لوحة المفاتيح وإمكانية الوصول (تعميق البند 41 من وثيقتك)
**المدة: 3-4 أيام**

وثيقتك ذكرت اختصارات لوحة المفاتيح (بند 41) لكن دون تفاصيل تقنية تنفيذية:

1. تنفيذ الاختصارات المذكورة عبر `InputBindings`/`KeyBinding` مركزية على مستوى `Window`، لا مبعثرة داخل كل شاشة.
2. دعم `Tab` navigation منطقي (`TabIndex`) في كل نموذج — مهم جدًا لموظف كاشير يعمل بالكيبورد بالكامل دون ماوس لسرعة الإدخال.
3. اختبار Narrator (قارئ الشاشة في ويندوز) على الأقل للشاشات الأساسية.
4. التأكد من تباين الألوان الكافي (خصوصًا للمبالغ المالية) وفق نفس معيار WCAG المستخدم في الأندرويد.

### معيار الإغلاق
- [ ] عملية بيع كاملة (بحث منتج → سلة → دفع → إتمام) قابلة للتنفيذ بالكيبورد فقط بدون لمس الماوس إطلاقًا.

---

## المرحلة 8 — الحزم والتوزيع (Packaging) — غائب تمامًا عن الوثيقة
**المدة: 3-5 أيام**

مهمة حرجة جدًا لتطبيق سطح مكتب يُوزَّع على عملاء حقيقيين (محلات) ولم تُذكر إطلاقًا في وثيقتك التصميمية:

1. اختيار آلية التوزيع: **MSIX** (الأحدث والأكثر أمانًا، يدعم التحديث التلقائي بسهولة) مقابل **ClickOnce** أو مثبّت تقليدي (Inno Setup/WiX) إن كانت هناك قيود توافق مع أجهزة عملاء أقدم.
2. آلية تحديث تلقائي (Auto-Update) — أساسية لأنك ستطور النظام باستمرار وعملاؤك محلات فعلية لا تراجع GitHub.
3. توقيع رقمي (Code Signing) للمثبّت — بدونه، ويندوز يعرض تحذير "ناشر غير معروف" يضعف الثقة بشكل كبير أمام عميل يشتري نظامًا "احترافيًا".
4. رقم إصدار (Versioning) واضح يظهر في شاشة "حول" (البند 33 في وثيقتك ذكر "About" كصفحة إعدادات — هذه فرصة لعرض رقم الإصدار وحالة التحديث).

### معيار الإغلاق
- [ ] مثبّت واحد بنقرة، بدون تحذيرات أمنية مخيفة، مع تحديث تلقائي يعمل فعليًا.

---

## المرحلة 9 — التنفيذ الفعلي وفق الـ 19 خطوة في وثيقتك (البند 68)
**المدة: الأطول — عدة أسابيع**

هنا تُنفَّذ خطوات وثيقتك (Audit → Function Extraction → Screen Mapping → App Shell → Dashboard → Sales/POS → ...) لكن بعد إنجاز المراحل 1-8 أعلاه كطبقة تحتية، بحيث لا تُبنى شاشة واحدة على أساس تقني غير محسوم (WPF أم WinUI؟) أو بدون Virtualization من اليوم الأول.

**نصيحة تنفيذية:** ابدأ بشاشة الـ **Dashboard والفواتير (Invoice Details/Print Preview)** بأعلى جودة أولاً — وثيقتك تشير لهما كنقطة ضعف واضحة في التصميم الحالي (البند 11)، وهما ما يراه أي عميل تجريبي أول مرة.

---

## جدول زمني تقديري إجمالي

| المرحلة | المدة |
|---|---|
| 0 — تنظيف | نصف يوم |
| 1 — حسم القرار التقني (WPF/WinUI) | فوري |
| 2 — Design System | 3-5 أيام |
| 3 — المكونات المشتركة | 1-1.5 أسبوع |
| 4 — الأداء | مستمر |
| 5 — الطباعة الاحترافية | 3-5 أيام |
| 6 — التوطين | 2-3 أيام |
| 7 — لوحة المفاتيح وإمكانية الوصول | 3-4 أيام |
| 8 — الحزم والتوزيع | 3-5 أيام |
| 9 — تنفيذ الشاشات (19 خطوة من وثيقتك) | 5-9 أسابيع |

**الأساس التقني (0-8) حوالي 3-4 أسابيع** قبل البدء الفعلي بإعادة بناء الشاشات وفق خطة وثيقتك.

---

## الخلاصة

وثيقتك تحدد **ماذا** يجب أن تبدو عليه الواجهة ولماذا الوضع الحالي غير مقبول (بمستوى تفصيل ممتاز فعلاً — 72 بند). هذه الخطة تضيف القرارات التقنية الحاسمة التي لم تُذكر: أي تقنية عرض (WPF/WinUI)، كيف يبقى الأداء سريعًا مع آلاف السجلات، كيف تُوزَّع النسخة على عملاء حقيقيين بثقة. بدون هذه الطبقة، حتى أجمل تصميم سيبقى Prototype لا يصمد في الاستخدام التجاري الفعلي.

**تذكير أخير:** لمراجعة تقنية أعمق (فحص فعلي لملفات XAML وC# الحالية بدل الاعتماد على الوصف فقط)، ارفع لي مجلد `src` أو نماذج من الملفات الحالية.
